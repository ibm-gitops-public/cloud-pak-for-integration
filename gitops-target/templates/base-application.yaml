apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cp4i
  namespace: argocd
spec:
  project: default
  source:
    repoURL: {{ .Values.giturl }}
    targetRevision: HEAD
    path: "{{ .Values.version }}"
  destination:
    server: https://kubernetes.default.svc