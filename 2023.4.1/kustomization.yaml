apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - platform-navigator

patches:
  - target:
      kind: CommonService
      name: common-service
    patch: |-
      - op: replace
        path: /spec/storageClass
        value: 'ocs-storagecluster-cephfs'
